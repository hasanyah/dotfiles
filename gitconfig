[init]
    defaultBranch = main
[alias]
    st = status
    br = branch
    co = checkout
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
    lgs = log --all --graph --decorate --oneline --simplify-by-decoration
[user]
    email = hasan.yahya@tover.care
    name = Hasan Yahya
[pull]
    ff = only
[core]
    editor = vi
[push]
	autoSetupRemote = true
[includeIf "gitdir:~/Personal/"]
    path = .gitconfig-personal
